# Descendant-aware smoke trigger

Temporary PR marker to rerun the canonical runtime smoke after hardening it to accept a deployed source commit that is newer than the commit seen when the smoke began. Do not merge.
