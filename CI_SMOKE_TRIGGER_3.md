# All-asset smoke trigger

Temporary PR marker to rerun canonical runtime validation after correcting the probe to inspect every deployed JavaScript asset rather than one arbitrary chunk. Do not merge.
